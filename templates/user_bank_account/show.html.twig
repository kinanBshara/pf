{% extends 'base.html.twig' %}

{% block title %}UserBankAccount{% endblock %}

{% block body %}
    <h1>UserBankAccount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_bank_account.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ user_bank_account.fullName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_bank_account.email }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ user_bank_account.balance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_bank_account_index') }}">back to list</a>

    <a href="{{ path('user_bank_account_edit', {'id': user_bank_account.id}) }}">edit</a>

    {{ include('user_bank_account/_delete_form.html.twig') }}
{% endblock %}
